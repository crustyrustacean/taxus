allow-branch = ["trunk"]
sign-commit = false
sign-tag = false
publish = false
push = false
pre-release-commit-message = "chore: release v{{version}}"
tag-message = "v{{version}}"
tag-name = "v{{version}}"